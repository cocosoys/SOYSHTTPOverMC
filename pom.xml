<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>soys.soyshttpovermc</groupId>
    <artifactId>SOYSHTTPOverMC</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>SOYSHTTPOverMC</name>

    <properties>
        <java.version>1.8</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <mcprotocollib.version>1.12.2-2</mcprotocollib.version>
        <protobuf.version>3.25.3</protobuf.version>
    </properties>

    <build>
        <finalName>SOYSHTTPOverMC</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.8.1</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.2.4</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                            </transformers>
                            <!-- 注意：opennbt 必须用 GitHub "GeyserMC/OpenNBT" 的 1.0 tag 旧版
                                 （含 com.github.steveice10.opennbt.tag.builtin.* 与 NBTIO），
                                 已被本地 mvn install 覆盖到 .m2 的 opennbt:1.0。
                                 Maven Central / JitPack 上同名 opennbt:1.0 是重构后的新版
                                 （扁平 tag + NBTIOUtils），与 MCProtocolLib 1.12.2-2 不兼容，
                                 会导致 Bot 登录时 ClassNotFoundException。无需 relocation。 -->
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>
    </build>

    <repositories>
        <repository>
            <id>spigot-repo</id>
            <url>https://hub.spigotmc.org/nexus/content/repositories/snapshots/</url>
        </repository>
        <repository>
            <id>jitpack.io</id>
            <url>https://jitpack.io</url>
        </repository>
        <!-- 直连 Maven Central(https)，绕过 alimaven 镜像缺失 protobuf-maven-plugin 的问题 -->
        <repository>
            <id>central-direct</id>
            <name>Maven Central (direct)</name>
            <url>https://repo1.maven.org/maven2/</url>
        </repository>
        <!-- AuthMe 发布仓库（5.4.0 等版本托管于此，不在 Maven Central） -->
        <repository>
            <id>codemc</id>
            <name>CodeMC</name>
            <url>https://repo.codemc.io/repository/maven-public/</url>
        </repository>
        <!-- Velocity API 发布仓库（com.velocitypowered:velocity-api 不在 Maven Central，
             由 PaperMC 代为托管；群组服伴随插件模块亦依赖此仓库） -->
        <repository>
            <id>papermc</id>
            <name>PaperMC</name>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>central-direct</id>
            <name>Maven Central (direct)</name>
            <url>https://repo1.maven.org/maven2/</url>
        </pluginRepository>
    </pluginRepositories>

    <dependencies>
        <dependency>
            <groupId>org.spigotmc</groupId>
            <artifactId>spigot-api</artifactId>
            <version>1.12.2-R0.1-SNAPSHOT</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>com.github.steveice10</groupId>
            <artifactId>MCProtocolLib</artifactId>
            <version>${mcprotocollib.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>io.netty</groupId>
                    <artifactId>netty-all</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <!-- MCProtocolLib 的传递依赖（本环境需显式声明以便从 jitpack.io / central 解析） -->
        <dependency>
            <groupId>com.github.steveice10</groupId>
            <artifactId>packetlib</artifactId>
            <version>1.2</version>
            <exclusions>
                <exclusion>
                    <groupId>io.netty</groupId>
                    <artifactId>netty-all</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>com.github.steveice10</groupId>
            <artifactId>mcauthlib</artifactId>
            <version>1.0</version>
            <exclusions>
                <exclusion>
                    <groupId>io.netty</groupId>
                    <artifactId>netty-all</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>com.github.steveice10</groupId>
            <artifactId>opennbt</artifactId>
            <version>1.0</version>
        </dependency>
        <!-- 关键：嗅探器要挂到 Spigot 自己的 Netty pipeline 上，必须复用 Spigot 运行时的 netty
             （同一 Class 对象），否则 io.netty.buffer.ByteBuf 的 instanceof 会因类加载器不同而失败。
             因此 netty 设为 provided（不打进 jar），运行时直接取 Spigot 自带的 4.1.24.Final。 -->
        <dependency>
            <groupId>io.netty</groupId>
            <artifactId>netty-all</artifactId>
            <version>4.1.24.Final</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>com.google.protobuf</groupId>
            <artifactId>protobuf-java</artifactId>
            <version>${protobuf.version}</version>
        </dependency>
        <!-- Lombok：编译期注解处理（@Data 等），provided 不打进 jar（运行时不需要） -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>1.18.36</version>
            <scope>provided</scope>
        </dependency>
        <!-- AuthMe 5.4.0：软依赖登录插件接入（provided：编译期校验 API，运行时由 AuthMe 插件自身提供类，
             因此不打进 jar；无 AuthMe 时本插件仍可正常运行，仅在 onEnable 自判 AuthMe 是否加载） -->
        <dependency>
            <groupId>fr.xephi</groupId>
            <artifactId>authme</artifactId>
            <version>5.4.0</version>
            <scope>provided</scope>
        </dependency>
        <!-- MySQL Connector/J：跨服同步数据存储（config.yml mysql 段启用时使用）。
             compile + shade 打入 jar：Spigot 1.12.2 仅内置 5.x 驱动（com.mysql.jdbc.*，连 MySQL 8 有认证兼容问题），
             自带 8.x（com.mysql.cj.*，与服务端 5.x 包名不同，无冲突）确保 MySQL 8 直连可用。
             排除其传递的 protobuf（X DevAPI 用，本项目不用，避免与 FrameProto 的 protobuf 版本纠缠）。 -->
        <dependency>
            <groupId>mysql</groupId>
            <artifactId>mysql-connector-java</artifactId>
            <version>8.0.28</version>
            <exclusions>
                <exclusion>
                    <groupId>com.google.protobuf</groupId>
                    <artifactId>protobuf-java</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <!-- 群组服（BungeeCord / Waterfall / Velocity）支持：
             仅编译期签名，provided 不打进 jar，后端 Spigot 独立运行不受影响；
             运行期经反射探测代理类，不在 classpath 时自动降级为独立服。
             后端侧探测纯反射 + Bukkit 配置 API，无需直接引用代理类型；
             BungeeCord 类型（及其传递依赖 brigadier SNAPSHOT）留待后续可选的
             「代理侧伴随插件」模块单独声明，避免污染主插件构建图。 -->
        <dependency>
            <groupId>com.velocitypowered</groupId>
            <artifactId>velocity-api</artifactId>
            <version>3.0.0</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>
</project>
