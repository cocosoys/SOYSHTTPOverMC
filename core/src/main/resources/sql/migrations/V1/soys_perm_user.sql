-- ==============================================================
--  V1 迁移：soys_perm_user 增加 player 检索索引
-- ==============================================================
--  迁移约定（SOYS 自动运维二期）：
--    1) 目录结构：sql/migrations/V<版本号>/<表名>.sql（版本号 = 子目录）
--    2) 仅 MySQL 方言后端执行（SQLite 运行时自动建表，不执行迁移脚本）
--    3) 执行记录写入 soys_schema_meta（plugin + tableName="*" 行）：
--       每个脚本【只执行一次】——中途失败不会重跑已成功的脚本
--    4) 升级路径：提高插件 schemaVersion（如 1→2）并新增 V2/<表名>.sql，
--       服务器启动（或 /soyshttp data <插件> update）自动增量执行
--    5) 本脚本为演示，若手动重复执行会因索引已存在而报错——属预期
--       （正常流程中由 meta 记录保证只执行一次）
-- ==============================================================
ALTER TABLE `soys_perm_user`
    ADD INDEX `idx_perm_user_player` (`player`);