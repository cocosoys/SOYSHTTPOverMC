# ============================================================================
# SOYSHTTPOverMC · 本地权限表（YAML 后端初始化模板）
# 表: soys_perm_user_group（用户 - 权限组关联）
# ----------------------------------------------------------------------------
# · 存储位置: <插件数据文件夹>/data/soys_perm_user_group.yml
# · 根节点:   soys_perm_user_group（= @TableName，与 SQL 表名一致）
# · 记录键:   记录 ID（= @TableId 主键）
# · 列名:     实体字段驼峰转小写下划线，与 SQL 列名一致
# · 运行时:   文件不存在时后端自动创建空表；本文件仅作为初始化模板 / 格式参考
# ----------------------------------------------------------------------------
# 字段说明:
#   id     记录 ID（主键）
#   uuid   玩家 UUID
#   group  权限组 ID（注意: group 为保留字，YAML 键名保持 group 即可）
# 示例记录:
#   "ug-1":
#     uuid: "01234567-89ab-cdef-0123-456789abcdef"
#     group: "admin"
# ============================================================================
soys_perm_user_group: {}
