# ============================================================================
# SOYSHTTPOverMC · KV 记录存储（YAML 后端初始化模板）
# 文件: <插件数据文件夹>/data/records.yml
#       （storage.backends.yaml.file 目录下固定文件名，勿改名）
# ----------------------------------------------------------------------------
# · 根节点:   records
# · 记录:     <key>: { type: <类型>, data: <序列化数据>, updated_at: <毫秒时间戳> }
# · 用途:     令牌黑名单 / 审计 / 心跳 / 全局密钥 等 KV 记录（无独立实体表）
# · 运行时:   文件不存在时自动创建；本文件仅作为初始化模板 / 格式参考
# 示例记录:
#   records:
#     "jwt:blacklist:xxx":
#       type: "STRING"
#       data: "1759000000000"
#       updated_at: 1759000000000
# ============================================================================
records: {}
